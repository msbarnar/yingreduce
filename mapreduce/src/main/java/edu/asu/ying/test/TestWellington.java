package edu.asu.ying.test;

/**
 * This is what I want the API to look like.
 * When this runs, I'm happy.
 */
public class TestWellington {

  /*
  // Start the p2p network on port 5000
  ResourceChannel transport = new P2PModule(5000);
  // Share RMI proxies over the p2p network
  ServiceProvider provider = new RMIServiceProvider(transport);
  // Get service connectors from RMI
  ServiceLocator locator = new ServiceLocatorImpl();
  locator.registerProvider(resource);

  // Get the DFSService for the page
  DFSService dfs = locator.get("mytable~0", DFSService.class);
  */
}
