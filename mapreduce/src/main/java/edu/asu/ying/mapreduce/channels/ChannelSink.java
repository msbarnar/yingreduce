package edu.asu.ying.mapreduce.channels;

import edu.asu.ying.mapreduce.messaging.MessageSink;

import java.io.Serializable;


/**
 * A {@link ChannelSink} is the <i>last</i> sink in an <i>outgoing</i> message sink chain and the
 * <i>first</i> in an <i>incoming</i> message sink chain.
 */
public interface ChannelSink
{
}
