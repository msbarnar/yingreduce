package edu.asu.ying.mapreduce.rmi.resource;

/**
 * The remote target of the {@link edu.asu.ying.mapreduce.rmi.resource.ResourceFinder}, the {@link ResourceProvider}
 * intercepts {@link ResourceRequest} and interprets the request.
 */
public interface ResourceProvider
{
}
